version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist  # Ensure this is the correct build output folder
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**